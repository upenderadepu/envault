package main

import (
	"fmt"
	"os"
)

func main() {
	// CLI commands will be wired in Phase 9 (internal/cli package).
	// For now, this is a placeholder that confirms the binary builds.
	fmt.Println("envault CLI - coming soon")
	os.Exit(0)
}
